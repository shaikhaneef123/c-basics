#include<stdio.h>
#include<string.h>
int main()
{
    char str[10];
    int i,len;
    scanf("%[^\n]s",str);
    for(i=0;str[i]='\0';i++)
    {
    	
	}
    len=i;
    printf("%d",len);
}